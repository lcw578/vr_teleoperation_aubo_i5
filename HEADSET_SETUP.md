# 头显侧准备清单（Quest 3 · VR 遥操作）

> 面向操作者的头显侧工作清单。PC 侧的准备（relay 安装、适配器、控制链）见
> [BASELINE.md](BASELINE.md) 与 [README.md](README.md) §10。
> 依据：_refs/vr-teleop-kit 的 README 与 relay 源码（Apache-2.0），2026-09-25 整理。

## A. 一次性准备（拿到头显当天，约 1–2 小时）

| 步骤 | 内容 | 时间 |
|---|---|---|
| 1. 开箱激活 | Meta 账号登录、连 **Wi-Fi（必须与 PC 同一局域网）** | 20 min |
| 2. 系统更新 | Quest OS 更新到最新（浏览器与 WebXR 支持随系统走） | 15 min |
| 3. 开发者模式（**仅 USB 路线需要**） | 手机装 Meta Quest App → 设备 → 头显 → 开发者模式。需要 Meta 开发者账号（免费，需验证，以 Meta 当时政策为准） | 20 min |
| 4. 物理安全环境 | 清出站立操作空间；画 Guardian 边界（防撞边界，与机械臂映射无关） | 10 min |
| 5. 手柄 | 装电池、确认双手柄可追踪 | 5 min |

**不做的事**：不侧载任何 APK、不装第三方 App——走浏览器 WebXR，Quest 自带浏览器即可。

## B. 两种连接方式（二选一，PC 上起同一个 relay）

| | **USB（推荐）** | **局域网** |
|---|---|---|
| 头显侧要求 | 开发者模式 + USB 调试授权（插线后头显里点"允许"） | 无任何要求 |
| PC 侧命令 | `vr-teleop-relay`（默认 127.0.0.1:8443）+ `adb reverse tcp:8443 tcp:8443` | openssl 生成自签证书（README 有命令），`vr-teleop-relay --host 0.0.0.0 --ssl-keyfile certs/key.pem --ssl-certfile certs/cert.pem` |
| 头显浏览器地址 | `http://localhost:8443/` | `https://<PC局域网IP>:8443/` |
| 首次警告 | 无（localhost 即安全上下文） | 接受"不安全"警告 |
| **延迟** | **~1 ms RTT、零抖动** | 可用，Wi-Fi 偶发 >100 ms 尖峰 |
| 约束 | 线长限制站位（站在 PC 旁） | 自由移动 |

建议先 USB 调通（延迟干净、测量可信），跑顺了再试局域网。

## C. 每次会话例行流程（5–10 分钟）

1. （USB）PC 重跑 `adb reverse`——换线/重插后必须重跑
2. 戴头显 → Quest 浏览器打开书签页
3. 首次：点 **Enter VR** 授权 WebXR 会话
4. **Calibrate wrist（每操作者一次）**：双手握紧侧键进入标定 → 手腕位置不动、转动手掌 5 秒
5. **Start Teleop** → 位姿流开始
6. 确认 Guardian 边界完好

## D. 操控语义（由 PC 侧适配器定义）

| 手柄动作 | 效果 |
|---|---|
| **按住 Grip（侧键）** | 离合接管——臂跟随；**松开 = 急停并锁存**（1 s 内冻结） |
| **扳机（食指）** | 夹爪开/闭切换 |
| **（预留）A/B/X/Y** | 缩放切换、E-stop、Detach 事件 |
| 双手握紧 | 腕部标定（同 C-4） |

## E. 安全与体感

1. **面向机器人做首次校准**——之后转身由映射器按头显位姿自动 yaw 修正（每次接合修正）
2. 任何异常：**松开 Grip**，臂 1 秒内冻结
3. 手柄电量影响追踪质量，会前充满
4. 站位：USB 路线站在 PC 旁；确保能看到机械臂或 RViz

## F. 头显侧不做的事

不侧载 APK、不装 App、不配置手势追踪（用手柄）、不装开发工具（USB 调试授权除外）。

## 已知待验证项（到货当天确认）

- Quest 3 浏览器对 vr-teleop-kit 页面的 WebXR 兼容性（标准 API，预期无问题，但未实测）
- Meta 开发者账号的验证流程以 Meta 当时政策为准
- 手柄 Grip 位姿的腕部支点标定效果（仓库作者已实现，需真人验证手感）

# VR 遥操调试方案（Quest 3 → 仿真 Aubo i5）

> 编制日期：2026-09-22
> **文档定位**：本方案是**执行文档**，指导把「Meta Quest 3 手柄 → 仿真环境中的 Aubo i5 机械臂」这条链路打通，并完成数据落盘与 LeRobot 衔接。
> **与其他文档的关系**：总方案见 [`VLA研究方案_2026-09.md`](VLA研究方案_2026-09.md)；本方案产出的数据集是主方案「阶段 1 专家演示采数」的前置条件；仿真环境侧的资产问题见 [`../04_仿真线/重建实验记录.md`](../04_仿真线/重建实验记录.md)。
> 标注约定：**【待验证】**= 需在本机实测确认；**（可选）**= 非必需路径。

---

## 1. 调试目标与适用范围

### 1.1 目标

**一句话**：在**不接触真机**的前提下，把「VR 遥操 → 位姿映射 → 逆运动学 → 仿真机械臂 → 数据落盘 → LeRobot 格式」这条完整链路跑通并调稳。

拆成四个可验收的子目标：

| # | 子目标 | 为什么重要 |
|---|---|---|
| G1 | **VR 环境通** —— Quest 3 手柄位姿能被 PC 稳定接收 | 这是所有后续工作的物理前提，且它**与机器人无关**，可以独立验证 |
| G2 | **映射通** —— 手柄动，仿真 Aubo i5 跟着动，方向正确、不跳变 | 验证"手柄 → 末端位姿 → IK → 关节"这条数学链路 |
| G3 | **手感可用** —— 能连续完成"接近 → 对准"这类精细动作 | 遥操质量直接决定采集数据的质量 |
| G4 | **数据通** —— 录制的 episode 能被 LeRobot 读入并用于训练 | 打通数据格式，避免后期返工 |

### 1.2 适用场景

- 真机**尚未就绪**（相机未装、驱动未通、或场地不可用），但想**提前把软件链路调通**
- 需要**零风险**地调试遥操映射（仿真里撞坏了没有代价）
- 操作员需要**先熟悉 VR 遥操**这种交互方式，再上真机
- 为后续的**仿真内数据增广**（MimicGen 式扩增）预先搭好环境

### 1.3 明确不属于本方案的范围（避免误解）

| 不覆盖 | 说明 |
|---|---|
| ❌ **不是"练操作手感"的训练计划** | 仿真到真机的手感迁移有限，尤其接触密集任务。本方案的价值是**打通链路**，不是**练人** |
| ❌ **仿真采的数据不直接作为训练素材** | 存在 domain gap。本方案的产物用于**验证流程、验证字段、验证格式**；真机数据仍需另采 |
| ❌ **不涉及相机与视觉观测** | 本阶段是纯本体状态的遥操链路；视觉观测在相机就位后接入 |
| ❌ **不做真机控制** | 真机侧的对接是阶段 E 的目标（只做准备，不执行） |

---

## 2. 系统与硬件组成

### 2.1 硬件

| 部件 | 型号/规格 | 本次用途 | 状态 |
|---|---|---|---|
| VR 头显 | **Meta Quest 3** | 遥操输入 + 第一人称画面反馈 | 已采购 |
| VR 手柄 | Quest 3 配套（左右各一） | 生成末端位姿命令；按键控制录制 | 随头显 |
| 主机 | PC（Windows 或 Ubuntu） | 跑 dora-rs 数据流、MuJoCo、数据落盘 | 待确认 |
| 网络 | Quest 与 PC **同一局域网** | UDP 传手柄位姿（默认端口 **5006**） | 待确认 |
| 机械臂 | Aubo i5（6-DOF） | **本次不参与**，只用其 MJCF 模型 | 已有模型 |
| 数据线 | USB-C（Quest 充电/开发用） | sideload APK、充电 | 随头显 |

> ⚠️ **本阶段不需要真机通电。** 但需要 Aubo i5 的 MJCF 模型文件（见 §3.4）。

### 2.2 软件栈（分层）

| 层 | 选型 | 说明 |
|---|---|---|
| **遥操层** | [`dora-openarm-vr`](https://github.com/enactic/dora-openarm-vr)（Apache-2.0） | Quest 3 → dora-rs 数据流；**同时支持 MuJoCo 与真机** |
| **数据流框架** | [dora-rs](https://github.com/dora-rs/dora) | 节点化数据流，YAML 编排；节点间用 Arrow 数组通信 |
| **仿真层** | MuJoCo（≥3.x） | 你们已完成 Aubo i5 的 URDF→MJCF 转换 |
| **IK** | dora-openarm 的 IK 节点（需按 Aubo 重配） | 6-DOF，无冗余 |
| **数据层** | [`dora-openarm-data-collection`](https://github.com/enactic/dora-openarm-data-collection) | 采数流程 + **输出可转 LeRobot 格式** |
| **训练层** | **LeRobot** | 数据格式与训练管线（ACT / Diffusion Policy / SmolVLA / π0 / π0.5） |

**备选遥操方案**（若主路径受阻）：

| 方案 | 出处 | 何时改用它 |
|---|---|---|
| **Open Teach** | NYU + Meta，arXiv [2403.07870](https://arxiv.org/abs/2403.07870) | 已支持 5 种机型（Franka/xArm/Jaco/Allegro/Hello Stretch），扩展机制更成熟；且**同时支持仿真与真机** |
| **Open-TeleVision** | CoRL 2024，arXiv [2407.01512](https://arxiv.org/abs/2407.01512) | 有 Isaac Gym 仿真示例；但它的"主动视觉"依赖可动头部相机，本项目用不上 |

### 2.3 架构与数据流

```
┌──────────────────┐
│  Meta Quest 3    │  头显内运行遥操 APK（sideload）
│  （手柄 + 头显）  │  手柄 6-DoF 位姿 + 扳机/按键
└────────┬─────────┘
         │ UDP :5006（同一局域网）
         ▼
┌──────────────────────────────────────────────┐
│  PC：dora-rs 数据流                           │
│                                              │
│   [Quest 输入接收节点]                        │
│            ↓                                 │
│   [位姿映射 / 增量计算]  ← 增量式，规避绝对标定  │
│            ↓                                 │
│   [IK 节点]  ← ★ 换成 Aubo i5 的 6-DOF 运动学  │
│            ↓                                 │
│   [MuJoCo 仿真节点]  ← ★ 换成 Aubo i5 MJCF     │
│            ↓                                 │
│   [状态发布] → 回到头显做画面反馈               │
│            ↓                                 │
│   [录制节点] → episode 落盘                    │
└──────────────┬───────────────────────────────┘
               ▼
      OpenArmDataset 格式
               ↓ 转换
        LeRobotDataset  →  LeRobot 训练管线
```

**★ 标记的两处就是"换成 Aubo i5"要改的地方**，其余部分（Quest 端、映射逻辑、录制与格式转换）可复用。

---

## 3. 调试前的环境与设备准备

### 3.1 Quest 3 侧

| # | 步骤 | 说明 |
|---|---|---|
| Q1 | 注册 **Meta Quest 开发者账号** | sideload APK 的前置条件 |
| Q2 | 安装 **Meta Quest Developer Hub**（MQDH） | 用于 sideload 与设备管理 |
| Q3 | 头显开启**开发者模式** | MQDH 内操作 |
| Q4 | **下载并 sideload 遥操 APK** | APK 下载链接见 dora-openarm-vr 的 README |
| Q5 | **关闭其他可能干扰的远程控制类 app** | 官方文档明确指出：其他远程控制 APK 可能抢占输入，导致收不到数据 |
| Q6 | **用胶带贴住头显中央的佩戴传感器** | 防止"戴在脖子上操作"时屏幕休眠。**这是官方给出的操作方式**：把头显挂在脖子上，用手柄操作 |
| Q7 | （Quest 3s 机型）在 MQDH 里将 **Proximity Sensor 设为 Disabled** | 同上目的 |

> 💡 **为什么要把头显挂在脖子上**：这个遥操方式的操作员**不戴头显**，而是用手柄对着空间操作，头显挂在脖子上只负责串流与追踪。所以 Q6/Q7 是必需步骤，不是可选优化。

### 3.2 PC 侧

```bash
# 依赖：Python ≥ 3.11、uv、dora-rs、MuJoCo

# ① 克隆遥操仓库（先不克隆数据采集仓库）
git clone https://github.com/enactic/dora-openarm-vr.git
cd dora-openarm-vr

# ② 构建 MuJoCo 版数据流
uv run dora build config/dataflow-mujoco.yaml --uv

# ③（阶段 D 用）克隆数据采集仓库 —— 注意必须带子模块
# git clone --recurse-submodules https://github.com/enactic/dora-openarm-data-collection.git
```

> ⚠️ **`--recurse-submodules` 不能省** —— 数据采集仓库依赖子模块，漏掉会缺节点。

### 3.3 网络（**最容易卡住的一环**）

| # | 检查项 | 方法 | 通过标准 |
|---|---|---|---|
| N1 | Quest 与 PC **在同一网段** | 分别查 IP | 前三段相同 |
| N2 | PC 的 IP 地址填入 Quest 端设置面板 | 头显内：左手柄菜单键 → 设置 → 填 IP + 端口 5006 | 面板显示已连接 |
| N3 | **防火墙放行 UDP 5006** | 临时关闭防火墙测试，或加放行规则 | `nc -lu 5006` 能看到数据 |
| N4 | **VPN 关闭** | 有 VPN 时先停掉 | 同上 |
| N5 | 端口未被占用 | 换端口试探 | 同上 |

**连通性验证（关键命令）**：

```bash
nc -lu 5006
```

**看到持续输出的数据 = 通了。** 没有任何输出，按 N1→N5 顺序排查。

### 3.4 机器人模型（你们已有）

Aubo i5 的 MJCF 由 `scripts/aubo_urdf_to_mjcf.py` 从官方 URDF 转换而来，已通过体检：

| 项 | 实测值 |
|---|---|
| 自由度 | nq=6 / nv=6 ✓ |
| 连杆数 | 7 |
| 总质量 | 21.99 kg（官方约 27 kg，量级正确） |
| 零位最大连杆距基座 | 1.008 m（米制正确，官方臂展 886.5 mm） |
| 产物 | `third_party/aubo_i5_mujoco/scene_aubo_i5.xml` |

> ⚠️ **这些产物在另一台 Linux 机器上**（`/home/lcw/tomato_robot/`），不在本仓库。**调试前必须先把 MJCF 及网格文件取回本机。**
> 转换时的两个已知坑（记录在案）：
> 1. `package://` 路径需改写
> 2. **视觉网格必须统一指向碰撞 STL**（MuJoCo 不支持 3ds/DAE）
> 3. **`<mujoco>` 必须是 `<robot>` 的子元素**，放外面会被解析成空 MJCF

**模型自检**（取回后立刻做）：

```python
import mujoco
m = mujoco.MjModel.from_xml_path("scene_aubo_i5.xml")
d = mujoco.MjData(m)
print("nq =", m.nq, " nv =", m.nv, " nbody =", m.nbody)
print("总质量 =", m.body_mass.sum(), "kg")
mujoco.mj_step(m, d)          # 能推进一步不报错
```

### 3.5 准备完成清单

- [ ] Quest 3 开发者模式已开，APK 已 sideload
- [ ] 其他远程控制 app 已关闭；佩戴传感器已贴住
- [ ] Quest 与 PC 同网段；防火墙放行 UDP 5006；VPN 已关
- [ ] `nc -lu 5006` 能看到数据
- [ ] PC 上 Python ≥3.11 / uv / dora-rs / MuJoCo 就绪
- [ ] `dora-openarm-vr` 已克隆并 build 成功
- [ ] Aubo i5 MJCF + 网格文件已取回本机，且自检通过

**以上全部打勾，再进入 §4。**

---

## 4. 调试步骤与操作流程

> **总原则：分层隔离变量。** 阶段 A 不改任何代码，只验证"VR 环境通不通"；通了之后再动"换机器人"这件事。这样出问题时你能立刻知道是哪一层的问题。

### 阶段 A · VR 环境验证（**不改任何代码**）

**目的**：用 OpenArm 的原版 demo 验证「Quest → UDP → dora-rs → MuJoCo」这条链。**此阶段跑的是 OpenArm 模型，不是 Aubo。**

| # | 操作 | 预期现象 |
|---|---|---|
| A1 | `uv run dora run config/dataflow-mujoco.yaml --uv` | MuJoCo 窗口打开，显示 OpenArm 机械臂 |
| A2 | 戴上手柄，**先缓慢扣一次扳机**做对齐 | 机械臂移动到位对齐姿态 |
| A3 | 移动手柄 | **机械臂跟随移动** |
| A4 | 转动手腕 | 末端姿态跟随变化 |
| A5 | 松开扳机 | 机械臂停止跟随 |

**验收**：A3–A5 表现符合预期 → 阶段 A 通过。

**若不通**：**不要往下走**，先按 §6 排查。这一层不通，后面所有工作都没有意义。

### 阶段 B · 切换到 Aubo i5

| # | 操作 | 检查点 |
|---|---|---|
| B1 | 把 Aubo MJCF 与网格放入工程，**先在 MuJoCo 里独立加载**（用 §3.4 的自检脚本） | 模型能加载、能 mj_step、关节数=6 |
| B2 | 在 dora 数据流的 MuJoCo 节点里**把场景指向 Aubo MJCF** | 运行后窗口里显示的是 Aubo i5 |
| B3 | **重配 IK 节点**，按 Aubo i5 的 6-DOF 运动学 | 见下方"6-DOF 的关键差异" |
| B4 | **对齐坐标系**：Quest 手柄位姿 → Aubo 基座坐标系 | 手柄向前推，末端向前；手柄上抬，末端上抬；**三个平移方向与三个旋转方向逐一验证** |
| B5 | 验证零位与工作空间边界 | 末端不超出基座可达范围；接近边界时不出现剧烈跳变 |

**6-DOF 的关键差异（必须注意）**

| | OpenArm（7-DOF） | **Aubo i5（6-DOF）** |
|---|---|---|
| 冗余自由度 | 有，可在保持末端位姿的同时优化肘部 | **无** —— 给定末端位姿后关节角**唯一**（解的分支有限） |
| IK 解空间 | 连续，易稳定 | **离散分支**（肩左/肩右、肘上/肘下），**必须做分支一致性约束**，否则会突然"翻臂" |
| 可达性 | 好 | **受限** —— 部分姿态不可达，IK 会失败 |

> **实操建议**：
> 1. **加"分支锁定"**：首次求解确定在哪个分支后，后续保持同分支，避免机械臂突然翻转
> 2. **IK 失败要"保持上一帧"而不是"跳到最近解"** —— 后者会造成危险的大幅瞬移
> 3. 可以用开源 IK 库辅助（如 `mink`，基于 MuJoCo 的任务空间控制），**（可选）**
> 4. 关节限位要按 Aubo i5 的实际限位设置，不能沿用 OpenArm 的

### 阶段 C · 映射与手感调优

| # | 调什么 | 建议起点 | 说明 |
|---|---|---|---|
| C1 | **增量式映射**（而非绝对） | **默认用它** | 记录手柄与末端的**初始位姿**，之后只映射 Δp / Δq。**这是规避"VR 世界系与机器人基座系绝对标定"的关键技巧**，HarvestFlex 用的就是这个思路 |
| C2 | 位置缩放因子 | **1:1** 起步 | 手柄移动 1 cm → 末端移动 1 cm。手太大/太敏感时再调 |
| C3 | 姿态映射 | 直接映射相对旋转 | 注意手柄坐标系与末端坐标系的轴向对应 |
| C4 | 平滑 / 限速 | 关节速度上限 + 一阶低通 | 防止抖动穿透到 IK 造成解跳变 |
| C5 | 死区 | 位置 ~2 mm、姿态 ~1° | 过滤手柄的微小噪声 |
| C6 | 工作空间限制 | 按 Aubo 可达包络设软限位 | 防撞（即使仿真里撞不坏，也要养成习惯） |
| C7 | 扳机语义 | 扣住 = 跟随；松开 = 停止 | 与 OpenArm 默认一致 |
| C8 | 状态重置 | 长按某键 → 重新取当前位姿为增量基准 | **必需**：手柄丢追踪后必须能重同步，否则会出现大幅跳变 |

**验收**：能连续完成"从就绪位姿 → 接近目标点 → 微调对准"这个动作，中途不丢跟踪、不跳变。

### 阶段 D · 数据落盘与 LeRobot 衔接

```bash
git clone --recurse-submodules https://github.com/enactic/dora-openarm-data-collection.git
cd dora-openarm-data-collection
uv venv .venv && source .venv/bin/activate
uv pip install dora-rs-cli
dora build dataflow-vr-mujoco.yaml --uv
dora run  dataflow-vr-mujoco.yaml --uv
```

| # | 操作 | 说明 |
|---|---|---|
| D1 | 录制开始时访问 `http://localhost:8000` 看采集状态 Web UI | 官方提供 |
| D2 | **按键映射**：A = 开始录制 / 标记成功；B = 停止 / 标记失败 | 官方默认 |
| D3 | 输出目录：`vr_mujoco_data`（可在 `dataflow-vr-mujoco.yaml` 的 `DIRECTORY` 改） | — |
| D4 | 录若干条后，**转换到 LeRobot 数据集格式** | 官方提供转换步骤 |
| D5 | **用 LeRobot 加载验证** | 字段完整、时间戳连续、能被训练脚本读入 |

> ⚠️ **从第一天就记录来源标签**。即使是仿真数据，也要在元数据里标明 `source: sim-rollout`、所用模型、映射参数。理由见主方案 §6.4 —— **来源标签事后补不回来**。

### 阶段 E · 为真机接口做准备

**目标**：写一层抽象，让"仿真后端"与"真机后端"共用同一套上层代码。

```
AuboI5Robot（抽象接口）
├── AuboI5MuJoCoRobot   ← 本方案已跑通的后端
└── AuboI5RealRobot     ← 后续实现（Aubo 官方 Python SDK / Ethernet）
```

| # | 操作 | 验收 |
|---|---|---|
| E1 | 定义统一接口：`connect()` / `get_state()` / `send_action()` / `disconnect()` | 接口稳定 |
| E2 | 把仿真实现包装成该接口 | 功能与阶段 C 一致 |
| E3 | 写一个**切换开关**（配置项） | 切换后端**只改一行配置**，不动上层代码 |
| E4 | 用 LeRobot 的 `Robot` 抽象对齐命名约定 | 便于后续接入 LeRobot 的录制/训练链路 |

---

## 5. 关键参数与指标

### 5.1 性能指标

| 指标 | **目标值** | 依据 |
|---|---|---|
| **端到端延迟**（手柄动 → 画面更新） | **< 100 ms** | HarvestFlex 实测 <0.1 s |
| **控制频率** | **30 Hz** | HarvestFlex |
| **数据录制频率** | 30 Hz | 与策略训练频率对齐 |
| UDP 端口 | 5006 | dora-openarm 默认 |
| IK 求解成功率（工作空间内） | **> 99%** | 低于此值说明限位/模型/求解器有问题 |
| 单条 episode 时长 | **10–30 s** | 番茄串单串任务的实际量级 |
| 位姿映射方式 | **增量式（Δp/Δq）** | 规避绝对标定 |

### 5.2 可调参数（手感相关）

| 参数 | 建议起点 | 调整方向 |
|---|---|---|
| 位置缩放 | 1 : 1 | 动作范围不够 → 放大；太敏感 → 缩小 |
| 姿态缩放 | 1 : 1 | 同上 |
| 位置死区 | 2 mm | 抖动明显 → 加大；迟钝 → 减小 |
| 姿态死区 | 1° | 同上 |
| 位置平滑系数 | 0.2–0.3（一阶低通） | 抖动 → 加大；滞后感强 → 减小 |
| 关节最大速度 | 按 Aubo 规格的 50% 起步 | 安全优先 |

> **调参原则**：一次只改一个参数，改完立刻试同一套动作（接近 → 对准），记录感受。**不要一次调多个。**

---

## 6. 常见问题及排查方法

### 6.1 VR / 网络侧

| 现象 | 可能原因 | 排查方法 |
|---|---|---|
| `nc -lu 5006` 无输出 | ① 不同网段 ② PC 的 IP 填错 ③ 端口不是 5006 ④ 防火墙挡 UDP ⑤ VPN 干扰 ⑥ **其他远程控制 APK 抢占输入** | 按 ①→⑥ 逐条排除。⑥ 是官方特别提示的坑 |
| 头显内看不到画面 | 头显休眠（佩戴传感器未贴 / Proximity Sensor 未关） | 贴住传感器；Quest 3s 在 MQDH 里禁用 Proximity Sensor |
| 手柄追踪丢失 | 手伸出视野、光线过暗、手柄电量低 | 保持手柄在头显追踪视野内；检查环境光；换电池 |
| 连上后姿态基准不对 | **戴在头上时连接、之后才挂到脖子上** —— 参考姿态会错位 | **先挂在脖子上、朝前，再连接**（官方明确提示） |

### 6.2 机械臂侧（换成 Aubo 之后）

| 现象 | 可能原因 | 排查方法 |
|---|---|---|
| 机械臂完全不动 | ① MJCF 未正确加载 ② IK 全部失败 ③ 数据流没接到 MuJoCo 节点 | 看 MuJoCo 日志有无 IK 失败刷屏；用 §3.4 脚本先独立验证模型 |
| **方向不对**（推前它往后） | 坐标系轴向不匹配 | 逐一验证 3 个平移 + 3 个旋转；建立明确的坐标变换矩阵并写进配置，**不要靠试凑** |
| **突然"翻臂"** | **6-DOF 的 IK 分支跳变**（7-DOF 不会遇到这个问题） | 加"分支锁定"约束（见 §4 阶段 B） |
| **大幅瞬移** | ① IK 失败后跳到最近解 ② 手柄丢追踪后重新捕获，增量基准失效 | 改为"IK 失败保持上一帧"；加"手动重同步"按键 |
| 持续高频抖动 | IK 解在相邻分支间来回跳；无平滑 | 加一阶低通 + 死区；检查是否接近奇异位形 |
| 某些姿态到不了 | 6-DOF 无冗余，可达性受限 | 属正常现象；限制操作范围，或改变基座朝向 |
| 运行一会儿卡死 | 数据流节点阻塞（MuJoCo 与 IK 同步阻塞） | 检查 dora 节点是否为异步；降低控制频率试探 |

### 6.3 数据侧

| 现象 | 可能原因 | 排查方法 |
|---|---|---|
| 录制目录为空 | 按键映射未触发；录制节点未启动 | 看 Web UI 状态；确认按的是 A 键 |
| LeRobot 读入报错 | 转换步骤遗漏；字段缺失 | 对照 LeRobotDataset 的必需字段逐项检查 |
| 时间戳不连续 | 录制节点与仿真节点频率不一致 | 统一到 30 Hz；检查是否丢帧 |
| 数据长度与预期不符 | episode 起止判定错误 | 复核按键语义（A = 开始/成功，B = 停止/失败） |

---

## 7. 验证与验收标准

### 7.1 分阶段验收

| 阶段 | 验收项 | 通过标准 |
|---|---|---|
| **A · VR 环境** | UDP 连通 | `nc -lu 5006` 持续有数据 |
| | 原版 demo 可跑 | 手柄移动 → OpenArm 模型跟随（平移 + 旋转都对） |
| **B · 换 Aubo** | 模型加载 | nq=6、能 mj_step、无报错 |
| | 跟随正确 | **3 个平移 + 3 个旋转方向逐一验证通过** |
| | 无分支跳变 | 连续操作 5 分钟，不出现"翻臂" |
| | IK 稳定 | 工作空间内求解成功率 >99% |
| **C · 手感** | 精细动作 | 能连续完成"接近 → 微调对准"≥10 次，无丢失 |
| | 延迟 | 目视无明显滞后（**建议实测**并记录数值） |
| | 重同步 | 人为遮挡手柄造成丢追踪后，按重置键能恢复且**不产生瞬移** |
| **D · 数据** | 落盘 | 录制目录生成 episode，Web UI 状态正常 |
| | 格式 | **LeRobot 能成功读入**，字段完整、时间戳连续 |
| | 来源标注 | 元数据里含 `source` / 模型版本 / 映射参数 |
| **E · 接口** | 抽象层 | 切换仿真/真机后端**只改一行配置** |

### 7.2 整体验收（一句话）

> **操作员用手柄在仿真里连续完成 10 次"接近目标点 → 对准"动作，全程无丢失、无跳变、无翻臂，延迟目视可接受；录下的数据能被 LeRobot 正常读入并用于一次训练跑通（哪怕只是过拟合级别的验证）。**

---

## 8. 风险与注意事项

### 8.1 安全

| 风险 | 说明 | 对策 |
|---|---|---|
| **操作员摔倒 / 碰撞** | 挂脖操作的场景下，操作员是站着挥动手柄的，可能碰到周围物体或失衡 | 清理操作区；划定站位；**不要在楼梯口或狭小空间操作** |
| 手柄甩出 | 快速挥动时手柄脱手 | 用手柄挂绳 |
| 长时间使用 | 挂脖姿势持续 1 小时以上可能不适 | 每次不超过 30 分钟，中间休息 |
| **仿真里"撞不坏"的错觉** | 仿真中无碰撞代价，容易形成危险的操作习惯 | **从第一天就按真机标准限定工作空间与速度**，养成习惯 |

### 8.2 技术风险

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| **6-DOF IK 分支跳变** | 高 | 高 | 分支锁定 + IK 失败保持上一帧（§4 阶段 B） |
| **UDP 被防火墙/企业网络拦截** | 中 | 高 | 阶段 A 先验证；必要时用有线直连或换端口 |
| 换 Aubo 模型时网格/路径出错 | 中 | 中 | 用 §3.4 的自检脚本先在 MuJoCo 独立验证 |
| 数据格式转换链路断裂 | 中 | 中 | 阶段 D 单独验收，尽早暴露 |
| **主路径（OpenArm 方案）不适用** | 低-中 | 中 | 备选 Open Teach（arXiv 2403.07870），其扩展机制更成熟 |
| dora-rs 生态较新、文档不全 | 中 | 中 | 遇到问题先看仓库 issue 与 Discord |

### 8.3 三个最重要的提醒

**① 不要把"仿真调通"当成"真机就绪"。**
仿真到真机还有一次映射调整：坐标系、通信延迟、动力学差异，以及最麻烦的**接触**。仿真调通的价值是**"软件链路通了"**，不是"操作练好了"。

**② 仿真采的数据不能直接当训练素材。**
存在 domain gap。本阶段的产物用于**验证流程与格式**。真机数据仍需另采（见主方案阶段 1）。**别把仿真 episode 混进真机数据集** —— 元数据里的 `source` 字段就是为此而设。

**③ 如果机械臂现在就能通电、能用 SDK 控制，考虑并行推进。**
真机那边调驱动，仿真这边调 VR 映射与数据落盘 —— 两条线不冲突，而且**阶段 E 的抽象层设计让两者最终能合流**。

---

## 附录 A · 一键检查命令集

```bash
# ── 1. VR 网络连通性（最常卡住的地方）──
nc -lu 5006                          # 有持续输出 = 通

# ── 2. 原版 demo（阶段 A，不改代码）──
git clone https://github.com/enactic/dora-openarm-vr.git
cd dora-openarm-vr
uv run dora build config/dataflow-mujoco.yaml --uv
uv run dora run   config/dataflow-mujoco.yaml --uv

# ── 3. 数据采集（阶段 D，注意子模块）──
git clone --recurse-submodules https://github.com/enactic/dora-openarm-data-collection.git
cd dora-openarm-data-collection
uv venv .venv && source .venv/bin/activate
uv pip install dora-rs-cli
dora build dataflow-vr-mujoco.yaml --uv
dora run   dataflow-vr-mujoco.yaml --uv
# 采集状态 Web UI: http://localhost:8000

# ── 4. Aubo i5 模型自检（阶段 B 前必做）──
python -c "
import mujoco
m = mujoco.MjModel.from_xml_path('scene_aubo_i5.xml')
d = mujoco.MjData(m); mujoco.mj_step(m, d)
print('nq=', m.nq, 'nv=', m.nv, 'nbody=', m.nbody, 'mass=', m.body_mass.sum())
"
```

## 附录 B · 参考资源

| 资源 | 链接 |
|---|---|
| dora-openarm-vr（VR 遥操，**主路径**） | https://github.com/enactic/dora-openarm-vr |
| dora-openarm-data-collection（采数 + LeRobot 转换） | https://github.com/enactic/dora-openarm-data-collection |
| OpenArm 生态总览 | https://github.com/enactic/openarm |
| OpenArm 数据采集文档 | https://docs.openarm.dev/tutorial/data-collection |
| dora-rs | https://github.com/dora-rs/dora |
| Open Teach（备选） | https://arxiv.org/abs/2403.07870 |
| Open-TeleVision（备选） | https://arxiv.org/abs/2407.01512 |
| LeRobot | https://github.com/huggingface/lerobot |
| MuJoCo | https://mujoco.org/ |

## 附录 C · 本方案中"可复用"与"要改"的界线

| 组件 | 状态 | 说明 |
|---|---|---|
| Quest 端 APK | **直接复用** | 只发手柄位姿，与机器人无关 |
| dora-rs 数据流架构 | **复用** | YAML 编排方式不变 |
| 位姿映射 / 增量计算逻辑 | **复用** | 与机器人型号无关 |
| IK 节点 | **★ 要改** | 7-DOF → 6-DOF，需加分支锁定 |
| MuJoCo 场景 | **★ 要改** | 换成 Aubo i5 MJCF |
| 关节限位 / 速度限制 | **★ 要改** | 用 Aubo i5 的实际参数 |
| 录制节点 | **复用** | — |
| 数据格式转换（→ LeRobot） | **复用** | 社区已有链路 |
| LeRobot 训练管线 | **复用** | ACT / DP / SmolVLA / π0 / π0.5 |

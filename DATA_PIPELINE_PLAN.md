# 数据与学习管线计划（番茄串演示 → π0.5）

> 2026-09-25 起草。范围：仿真内录制番茄串采收演示 → LeRobotDataset → 本机小模型练手 →
> 云端 π0.5 LoRA 微调 → 策略回灌仿真闭环验证。
> **不在本计划内**：真机部署、剪切末端复刻、两侧相机扩展（阶段 G 后视结果再议）。
> 上游依据：样机论文《Design, Development, and Field Testing of a Tomato Bunch Harvesting Robot》
> Xu et al., JFR 2026（`~/tomato_robot/docs/papers/00_基准与直接先例/`）。
>
> **【定位声明，2026-09-25 用户裁定】当前仿真 = 管线测试床**：①夹爪后续更换，AG95 仅为占位——
> 数据集夹爪通道保持 1 维归一化开合指令，换夹爪=换资产+重录，管线不动；②VR 头显未到货，
> 键盘是近期唯一演示输入；③接触物理以"能抓起、能落篮"为准，不做严谨调参。
> 正式数据集等 VR 到货 + 夹爪定型后再录；此前所有接触/负载类不明确点按此口径降级。

---

## 0. 已锁定事实（不再讨论，均有出处）

| 事实 | 出处 |
|---|---|
| 底盘 1.30×0.84×0.49 m、基座离地 1.2 m、Aubo i5 | `scene_ros2.xml` 注释（建库时已按样机论文 Table 4 落库） |
| **【一代参考】** 相机为 eye-to-hand 固定式；单侧安装距臂基 310 mm、倾角 14°、高 400 mm、彩色 FOV 70°×43° | 样机论文 3.1.3 节 + Figure 6（该倾角比 0° 多收 16.43% 可采收番茄，4.1 节） |
| 种植面距臂基 800 mm；果梗采收点高 1100–1500 mm → sim 坐标 z∈[−0.1,+0.3]；臂前伸 ~0.85 m < 944 mm 最大采收距离 | 论文 2/3.1.3 节 + 几何换算 |
| **【二代配置，2026-09-25 用户裁定】腕部相机 D405（eye-in-hand）+ 左右侧 D435 ×2（eye-to-hand）**。一代论文的 L515 安装参数降级为参考值——二代侧相机安装位未定，仿真先渲染候选机位再定。内参换算：D405 彩色 1280×720 → **fovy=58.0°**（HFOV 89.1°）；D435 彩色 640×480 → **fovy=42.6°**（HFOV 54.9°）。内参 JSON 收录于 `assets/camera_intrinsics/`（D435 畸变系数全 0；D405 k1=−0.054、主点偏移 13 px） | 用户 + 本机换算 2·atan(h/2/fy) |
| **VLA 数据采集不需要手眼标定**：策略从 (像素, 本体状态, 动作) 配对中自己学相机↔臂的相对关系，不消费任何外参；仿真里更是零标定（虚拟相机一切精确已知）。内参的唯一用途 = 让虚拟相机几何**匹配**真机（同 FOV/分辨率），这是匹配不是标定。取而代之真正要保证的三样：①安装刚性（装好不许动，动了数据分布就漂）；②时间戳对齐；③（真车端可选）实拍图 rectify 成针孔以匹配仿真渲染 | 本计划分析 |
| 虚拟相机可行：EGL 离屏渲染已用 scene_ros2.xml 实测通过（系统 python 3.3.5） | 2026-09-25 探针，图在 /data/viz/cam_probe/ |
| lerobot 环境就绪：/data/lerobot @ e624f3f7、数据格式 v3.0、`lerobot-run` 入口、hf-mirror、端到端冒烟已过 | 2026-09-25 环境会话 |
| mujoco_ros2_control 无相机发布、无通用 model 访问接口；上游不改（溯源规则） | 源码审查 + 依赖溯源规则 |
| π0/π0.5 本机不可训（8 GB vs LoRA >22.5 GB）；云端训练、云/本地推理 | openpi README + 本机实测 |

---

## 1. 任务定义（演示闭环）

**任务**：从种植架"采收"一串番茄 → 放入底盘果篮。
**分解**：够近 → 夹取果簇 → 搬运 → 松爪落篮（对应论文 Figure 18a–f 去掉剪切步）。
**VLA 语言指令**：`Pick up the tomato bunch and place it in the basket.`（π0 预训练为英文指令）

场景构件：
- 种植架（static）：竖直平面在 x=+0.8 m（臂基坐标系），含托板/横杆
- 番茄串 ×3–5：果梗 + 6–8 颗果（复用 `~/aoc_tomato_farm/.../tomato_0/meshes/tomato.dae`），单串质量按论文 250–500 g 设定
- 果篮：底盘上（论文 Figure 11 蓝筐位置），内壁加高防弹出
- **同步规则**：新增刚体必须同步 `add_scene_floor.py` 的 OBJECTS 表（scene_ros2.xml 注释自带的要求）

### 1.1 【关键分叉】抓取物理的实现路径 ⚠️

**进程边界问题**（本次审计最大发现）：夹爪 FSM 是 ROS 节点，主 sim 在 mujoco_ros2_control 进程里，
**没有任何上游接口能从外部修改 MjModel**（如 weld 的 eq_active）。

- **方案 A（采纳，先做）**：番茄串**搁置**在种植架托板上（不悬挂、不焊接），抓走即走。
  零上游侵入、纯 Python、无进程通信问题。抓取目标 = **果簇**（多球体，好抓，
  同时避开 4 mm 果梗 AG95 难夹的坑）。挂秧的视觉感靠托板+竖杆场景弥补。
- 方案 B（二期，挂秧式）：需要小 C++ 扩展插件（仿 `mujoco_3d_lidar` 模式订阅 ROS 话题切
  eq_active）。等方案 A 跑通、且"剪切末端复刻"立项时一并做——那时任务形态本来就要重设计。
- 方案 C（否决）：直接改 mujoco_ros2_control 上游副本——破坏依赖溯源规则。

---

## 2. 阶段 A：任务场景构建（估 1–2 天）

1. 检查 `tomato.dae` 实际尺寸/单位 → 缩放到樱桃番茄规格（果实 Ø15–25 mm 量级）
2. 建 bunch：果梗 + 果簇（**碰撞体用简化球组**，不用 DAE 原网格做碰撞）；质量按论文 250–500 g
   （同时查 AG95 额定负载确认可搬运，超出则减果数）
3. 种植架 + 托板（static）+ 3–5 串摆放（不同高度 z∈[−0.1,+0.3]、不同朝向）
4. 果篮（底盘上）+ 内壁防弹跳；接触参数（指↔果、果↔篮）按需调 solref/friction
5. 同步 `add_scene_floor.py` OBJECTS
6. 用方块先行验证抓放全链（沿用既有计划），再换番茄串

**验收**：键盘遥操（run_teleop.sh）完整跑通一次"够近→夹取→搬运→落篮"，
全程无异常接触告警，bunch 不穿模、不掉地。

---

## 3. 阶段 B：虚拟相机（估 2 天，可与 A 后半并行）——按二代配置 3 路

1. MJCF 加 camera 元素（内参来自 `assets/camera_intrinsics/`，均为针孔近似）：
   - **D405 腕部（eye-in-hand）**：挂在腕部/AG95 基座上的支架 body，朝夹爪工作区，
     `fovy=58.0°`、渲染 1280×720。D405 是短距相机（理想工作距 ~0.07–0.5 m），装腕部正合适；
     **安装位姿二代还没定 → 先渲染候选位姿挑**。顺手加一个小盒 geom 当相机外壳——
     真机腕部画面里必然有外壳/夹爪入镜，仿真不含会留 sim-to-real 缺口
   - **D435 左右双目（eye-to-hand，HarvestFlex 式同侧布置，2026-09-26 用户纠正）**：
     沿行轴分立作业区两侧（x=±0.40，桅杆高 0.25），都朝内看**同一片工作区**——
     视角冗余（HarvestFlex Table VI：单视角→双视角 SR 10%→42%）。两路都渲染并录制进数据集 v1。
     ~~旧理解（一代论文轨道镜像，已作废）~~
     安装高度/倾角二代未定 → 沿用一代论文的参数化思想（左右视宽均衡、初始位不互挡）
     + 候选机位渲染对比后定
   - MuJoCo 相机是中心主点、零畸变的针孔模型：D405 的 13 px 主点偏移与畸变不建模
     （**v1 近似**）；若迁移时发现敏感，优先在真车端把实拍图 rectify 成针孔（而不是给仿真加畸变）
2. sidecar 渲染节点（纯 Python ~200 行）：订阅 /joint_states → qpos 回放 → mj_forward →
   EGL 渲染 3 路 → `sensor_msgs/Image + CameraInfo`（CameraInfo 直接填内参 JSON 的数值）
   - 构建时**实测** /joint_states 的发布频率与 header stamp 源（sim time or wall time）
   - MUJOCO_GL=egl 写进 launch；渲染节点的 mujoco 用 3.3.5（已验证加载该场景 OK）
3. 验收：**FOV 覆盖率**——侧相机全程覆盖所有串（对照论文 4.1 节验收思想）；
   腕部相机在抓取位姿覆盖果簇与夹爪指；渲染画面与 viewer 一致性抽查

---

## 4. 阶段 C：录制管线（搭 0.5–1 天；演示耗时另计）

- rosbag2 落 `/data/rosbags/`。话题清单：`/joint_states`、相机 Image(**compressed**)+CameraInfo、
  servo target 流、夹爪 FSM 事件、TF
- **随机化方案**（规避进程边界）：预生成 5–10 份场景变体 XML（串位置/朝向抖动）轮换启动；
  兜底 = viewer 里手拖 bunch
- 体积估算：3 路相机（720p + 480p×2 jpeg）≈ 15–30 MB/s；单条 episode 预计 1–2 min（servo 慢，首录实测）；
  50 条 ≈ 60–200 GB，/data 可容
- 演示输入：**键盘先用于管线验证**；正式数据集建议等 VR 手柄接入（键盘轨迹偏糙，影响模仿学习质量）

**验收**：连录 3 条不同起点的完整采收 bag，回放无掉帧、时间戳单调。

---

## 5. 阶段 D：转换器 rosbag2 → LeRobotDataset（估 1 天 + spike）

- **跨 Python 环境问题**：rosbag2_py 在 ROS 的 3.10，lerobot 写库在 3.14。
  首选 pip `rosbags`（纯 python 解析 bag，**需 spike 验证 Humble 默认 sqlite3 格式**）；
  兜底两段式（3.10 dump 中间 npz/parquet → 3.14 写数据集）
- action 定义（**训练时定，录制两种全存**）：measured next-state（与 pi05 relative-actions 匹配，
  推荐）vs servo 指令流
- fps=30（与外环 P=30 匹配）；视频编码 torchcodec/PyAV（已验证）；任务指令写入 tasks
- **验收**：`lerobot-dataset-viz` 逐条回看 + 统计检查（states 连续、无 NaN、action 幅值合理）

---

## 6. 阶段 E：本机小模型练手（估 1–2 天）

- `lerobot-train --policy.type=act`（几十 M 参数，8 GB 可训）
- 评估先**离线**：用录制的 states 喂 policy，对比其动作与人类动作（幅值/相位）；
  闭环 eval 放阶段 G 的桥一起做

---

## 7. 阶段 F：云端 π0.5 LoRA（探针 0.5 天 + 训练按小时计）

1. **显存探针**：公开数据集上 10 min LoRA，实测 lerobot-main 的 pi05 显存（无官方数字，
   openpi 的 22.5 GB 是 JAX 版）
2. 环境上云：整仓 + `uv.lock` + 数据集（AutoDL 类 4090）
3. 训练配置：`--policy.type=pi05 --policy.use_relative_actions=true`（夹爪保持绝对，
   与我们 configuration-space 设定一致）

---

## 8. 阶段 G：策略回灌 sim 闭环

- **部署桥**：policy 推理需要 Python ≥3.12 → 不能进 ROS 环境 → 独立进程（lerobot env）+
  zmq/桥，动作 chunk → servo target 流（顶替 mock_vr 的接口位置）
- **spike**：lerobot 自带 `async_inference`/`transport` 模块能否直接当服务端用（不确定，需调研）；
  兜底自写 zmq 包装
- 验收：闭环采收成功率、与人类演示的动作质量对比

---

## 9. 不明确点总表（审计产出）

| # | 不明确点 | 影响 | 决策/解决时机 | 兜底 |
|---|---|---|---|---|
| 1 | weld/detach 的进程边界（本审计新发现） | 任务形态 | **现在**（选方案 A） | 方案 B 插件 |
| 2 | `rosbags` 能否解析 Humble sqlite3 bag（新发现） | 转换器架构 | 阶段 D 开工 spike | 两段式转储 |
| 3 | /joint_states 频率与 stamp 源（新发现） | 相机同步质量 | 阶段 B 实测 | — |
| 4 | AG95 夹果簇的接触参数调优 | 抓取成功率 | ~~阶段 A~~ **按测试床口径降级**：调到能抓即可 | 宽松摩擦；方块先行 |
| 5 | 落篮弹跳 | 成功判定 | ~~阶段 A~~ **降级**：能落进去即可 | 内壁加高 |
| 6 | ~~AG95 额定负载 vs 串重 250–500 g~~ | ~~搬运动力学~~ | **作废**（夹爪将更换，AG95 仅为占位） | 减果数 |
| 7 | 单条 episode 实际时长 | 数据量/工作量 | 阶段 C 首录 | — |
| 8 | 动作定义（measured vs commanded） | 训练效果 | 阶段 D/E | 两种全录 |
| 9 | 键盘演示平滑度不足 | 数据质量 | 阶段 C | 等 VR 接入 |
| 10 | lerobot-main pi05 LoRA 实际显存 | 云端路线 | 阶段 F 探针 | openpi 路线（需 v2.1 格式转换） |
| 11 | π0.5 服务化路径 | 部署 | 阶段 G spike | 自写 zmq 桥 |
| 12 | tomato.dae 实际尺寸/单位 | 真实感/碰撞 | 阶段 A 检查 | 缩放 |
| 13 | 二代 D435 侧相机安装高度/倾角未定 | 侧相机 FOV | 阶段 B 候选机位渲染对比 | 沿用一代论文参数化思想（左右视宽均衡） |
| 14 | 二代 D405 腕部安装位姿未定 | 腕部 FOV/遮挡 | 阶段 B 候选位姿渲染 | 挂 AG95 基座侧向前、可调支架 body |
| 15 | D405 畸变（k1=−0.054）与主点偏移 13 px 未在仿真建模 | sim-to-real 视觉域差 | 迁移出问题再处理 | 真车端 rectify 成针孔（推荐）；或仿真端加畸变后处理 |
| 16 | 腕部相机画面里的外壳/夹爪自遮挡 | sim-to-real 观测一致性 | 阶段 B | 仿真加相机外壳 geom |

---

## 10. 顺序与依赖

```
A 场景 ──→ B 相机 ──→ C 录制 ──→ D 转换 ──→ E 练手 ──→ F 云端π0.5 ──→ G 闭环
   └────────(A 的方块版可与 B 并行；B 的 FOV 验收依赖 A 定稿)────────┘
```

- 阶段 A+B 完成即可开始 C；E 完成即有"全链路已验证"的底气；F/G 与 E 可交叉。
- 所有阶段产物落 /data（bag、数据集、权重）；仓库只进代码与文档。

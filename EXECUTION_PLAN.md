# 执行计划：把仿真链路对齐到开源组件，再重做测量

> 建立：2026-09-24。上一版基线见记忆里的 `baseline-2026-09-24-issues`。
> **本轮目标不是"把跟随调好"，而是先把链路换成与真机同构、且由开源组件承担的形式**，然后才做量化测量。
> 顺序有依赖：几何没修 → 位姿集不可用 → 测出来的数字没有意义。

---

## 0. 前提与边界（已裁定，不再讨论）

| 项 | 裁定 | 依据 |
|---|---|---|
| 控制模式 | **位置模式**（唯一路线） | 真机不支持速度控制：厂家驱动的速度命令接口是死壳，`write()` 只调位置型 `Servoj` |
| "柜子"那一环 | **用开源组件，不用自写节点** | `gazebo_ros2_control` 与 `mujoco_ros2_control` 都内置每关节 `control_toolbox` PID；手写的 `servo_interface.py` 是在替代它 |
| 作业区 | 论文给的任务区：正前方、距基座 **700–944 mm**、垂直偏移 ±501 mm 以内、工具轴近水平 | Xu et al., J. Field Robotics 2026（番茄串采摘机器人，Aubo i5） |

**本轮明确不做**：不接 VR、不碰真机、不做数据记录、不改奇异阈值（阈值与我的离线指标不一致，见第 6 节的遗留项）。

---

## 1. 修几何：地板与基座的关系

**问题（已定位，有证据）**：仿真里**机械臂基座就在地板上（z=0）**，而真机基座装在升降平台上、地面在下方约 1.0–1.6 m。于是按任务区（相对基座 |z| ≤ 501 mm）生成的位姿有**一半落在地板以下**，控制器忠实地把臂往地里开。
证据：抬高阈值那次运行的逐样本记录里 `碰撞降速` 1596 次、`碰撞急停` 24 次、`关节到界` 9800 次、|关节速度| 峰值 **7.13 rad/s**（远超手册上限 2.618/3.142）。

**依据**：论文给出果实离地 1100–1500 mm，且机械臂可采距离 ≤944 mm、水平距离 y₀=800 mm → 基座必须在果实高度 ±501 mm 内 → **基座离地约 1000–1600 mm**。

**动作**
- `assets/aubo_i5/scene_ros2.xml` 与 `scene_ros2_velocity.xml`：地板 geom `pos="0 0 0"` → **`pos="0 0 -1.2"`**（取 1000–1600 mm 的中值；文件里注明这是"升降平台的设定高度"，可调）
- 加注释说明它与真机的对应关系，避免以后被当成随手改的数

**验证（这是本步的判据）**
- 离线加载模型，确认 floor 的 z = −1.2
- 在新 home 位姿下 `ncon == 0`
- **把任务区里的候选位姿逐个（以及它们之间的路径）走一遍，全程 `ncon == 0`**（夹爪抓取除外）

**完成判据**：任务区内任意位姿及其路径都不与地板接触。

---

## 2. 统一两个场景的起始位姿

**问题**：`scene_ros2.xml` 的 `ready` 关键帧已换成新 home（`-1.089254 -0.802598 1.308255 0.588418 0.324607 -0.704605`，末端距基座 784 mm、正前方），而 `scene_ros2_velocity.xml` **还是旧 home**（`0 -0.4 0.8 0 0.4 0`，末端 890 mm、侧前方）。两个场景起始位姿不一致，对照实验不可比。

**动作**：把速度场景的 keyframe 也改成同一组；并在文件里注明"速度场景已归档，仅为对照，keyframe 与位置场景保持一致以免不可比"。

**验证**：离线加载两个场景，比较 `key_qpos` 与 `key_ctrl`；`go_ready.py` 的 `READY` 常量与两者一致。

**完成判据**：三者（两个 keyframe + go_ready 的 READY）严格一致。

---

## 3. 把位置执行器换成 `<motor>` + 内置 PID

**为什么**：`gazebo_ros2_control` 与 `mujoco_ros2_control` 的"位置环"都是**每关节 `control_toolbox` PID**（前者 `Pid`，后者 `PidROS`）。而 MuJoCo 原生 `<position>` 执行器是**纯 PD、没有积分项**——这就是"Servo 的位置流追不上"的结构原因（实测速度增益只有 0.140–0.753）。积分项（或真机柜子的速度前馈）才是那一环的正解。

**动作**
1. `assets/aubo_i5/actuators_position.xml`：6 个**臂**关节 `<position kp dampratio>` → **`<motor>`**，`ctrlrange` 取厂家力矩上限 `protect_max_torque`（80/80/60/16/16/10 N·m）。
   **夹爪保持 `<position>`**（tendon 执行器，不参与本次改动）。
2. 新增 `ros2_ws/src/aubo_i5_teleop/config/mujoco_pid.yaml`：
   ```yaml
   /**:
     ros__parameters:
       pid_gains:
         position:
           shoulder_joint:  {p: …, i: …, d: …, u_clamp_max: 80.0, i_clamp_max: …}
           … 6 个关节
   ```
   - **`i` 必须 > 0**（这是修"追不上"的关键）；`i_clamp_*` 就是**抗积分饱和**，替掉手写层的 `lag_max`
   - `u_clamp_*` 取关节力矩上限
   - 起点：p 参考官方示例 `config/mujoco_pid.yaml` 的量级，d 同；i 先用小值
3. `launch/stage2a_mujoco.launch.py`：把 `mujoco_pid.yaml` 加进 ros2_control 节点的 `parameters`
4. **删除** `scripts/servo_interface.py` 及 `stage3_pose_tracking.launch.py` 里的节点接线；Servo 的 `command_out_topic` 改回 `/forward_command_controller_position/commands`
5. `CMakeLists.txt`：去掉 `servo_interface.py` 的安装项

**验证**
- 离线：确认 motor 执行器被编成 `biastype=NONE`（插件据此判为 MOTOR）
- 起栈：日志出现 `Joint <name>: position control enabled`，且 PID 真的在算（不是 pass-through）
- 行为：**2 cm/s 跟踪 ≈ 100%**（与手写层当时的结果对齐——这是"开源组件能替代它"的判据）

**完成判据**：整栈起来、`go_ready` 归位误差 = 0、2 cm/s 跟踪 ≥ 95%。

---

## 4. 按新几何重生成位姿集

**动作**（改 `scripts/gen_bench_poses.py`）
- 保留：关节角约束 `|q| ≤ 1.6`、**路径条件数检查**（从 home 插值过去，路径最大值 < 150）、条件数分层、几何分散
- 新增：**碰撞检查**——每个候选位姿及其路径上 `ncon == 0`（地板已降到 −1.2，这条现在是真检查而不是被几何逼出来的）
- 任务区定义不变（700–944 mm、y∈[−0.9,−0.7]、垂直偏移 ±0.501 m 以内、工具轴近水平）

**验证**：反向校验（从 q 重算 FK 与条件数，必须一致）+ 打印每个位姿的 `ncon` 与路径最大条件数。

**完成判据**：8–12 个位姿，每个都满足"路径无碰撞 + 路径条件数 < 150"，且条件数覆盖 3 个以上量级层。

---

## 5. 标定 PID 增益

**为什么单独一步**：官方示例的增益是给它自己的 demo 机器人的，不能照抄。

**动作**（离线台架，复用 `scripts/position_actuator_kp_calib.py` 的框架，对象换成"motor + PID"）
- **主指标：速度增益 → 1.0**（这是"追不上"的直接度量；上一轮的仪器坑：必须用**每周期位置增量**算速度，不能用周期末瞬时 qvel——硬伺服会在周期内振荡、末端采样会混叠）
- 次指标：超调、末段振荡（峰峰值）、`u_clamp` 触发比例
- 扫描顺序：先 p（把环路立起来）→ 再加 i（消除稳态误差）→ 最后 d（压超调）

**完成判据**：各关节速度增益 ≥ 0.95、无持续振荡、`u_clamp` 不长期饱和。结果写进 `mujoco_pid.yaml` 的注释。

---

## 6. 测三张表（安静度 / 带宽 / 延迟）

**动作**：`scripts/run_bench.sh` 一键（顺序已修正：**stage2a → stage2b → go_ready → stage3**；`go_ready` 必须在 stage3 之前，否则它的命令会被位置环覆盖——踩过）。

**完成判据**：三张表齐备；复跑一遍看重复性；每条结论都能指到具体位姿与条件数。

---

## 遗留项（本轮不处理，但要记在案）

1. **奇异阈值与离线指标不一致**：阈值 200 下，我离线算条件数 19.8–128.5 的位姿被 Servo 判为"急停"。→ **不能用我的 σmax/σmin 指标去定阈值**；要用实测转换点锚定，或改用"工作区可达性"这种可直接检验的判据。
2. **厂家手册的关节速度上限是 150°/s (关节1-3) = 2.618 rad/s、180°/s (4-6) = 3.142**，而我们的 `joint_limits.yaml` 用 **3.15 / 3.2**（超出 20%）。→ 要改（但改它会影响 MoveIt/Servo 的速度限幅，需单独一步验证）。
3. **`servo_interface.py` 的验证价值要保留**：它证明了"位置流必须补上积分/前馈那一环"（同一被控对象：补上 1.000、不补 0.758–0.14）。实现作废，结论保留。
4. **真机侧未验证**：servoj 是否真做速度前馈、是否限制加加速度（`servoJoint(traj, a, v, t, lookahead, gain)` 的参数含义在未获取的 SDK 文档里）。

---

## 需要拍板的两点

1. **地板降到 −1.2 m** 是否可接受？（它代表升降平台的设定高度，真机范围 1000–1600 mm；这个值决定了任务区相对地面的高度，换值只改一个数）
2. **PID 增益初值**我拟按"官方示例量级 + 我们的力矩上限"推、然后按第 5 步标定——是否接受"先跑起来再标"？
